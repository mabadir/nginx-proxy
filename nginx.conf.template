events {
  worker_connections  1024;
}

http {
  server { 
    listen ${PORT};

    location /api {
        proxy_pass ${API_HOST};
      }

    location / {
        proxy_pass ${WEB_HOST};
      }
  }
} 